name: Monitor

on:
  schedule:
    - cron: '* * * * *'

jobs:
  ping_site:
    runs-on: ubuntu-latest
    name: Ping the site
    steps:
    - name: Check the site
      id: hello
      uses: srt32/uptime@master
      with:
        url-to-hit: "https://flightdotapi.azurewebsites.net"
        expected-statuses: "200,301"
